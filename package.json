{
  "name": "water-sort-puzzle",
  "version": "1.0.0",
  "description": "Premium Water Sort Puzzle Game",
  "main": "index.html",
  "scripts": {
    "build": "powershell -Command \"if (!(Test-Path www)) { New-Item -ItemType Directory -Path www }; Copy-Item index.html, style.css, game.js -Destination www -Force\""
  },
  "dependencies": {
    "@capacitor/core": "^5.7.0",
    "@capacitor/android": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
